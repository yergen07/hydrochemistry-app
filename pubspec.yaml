name: hydrochemistry_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^1.10.17
  intl: ^0.19.0
  file_picker: ^6.1.1
  url_launcher: ^6.2.4
  flutter_svg: ^2.0.10+1
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/logo_lab.png
    - assets/logo_kz.png
